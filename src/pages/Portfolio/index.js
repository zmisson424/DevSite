import React, { Component } from "react";
import styled from "styled-components";
import { colors } from "../../config";

class Portfolio extends Component {
  render() {
    return <h1>OSU Portfolio</h1>;
  }
}

export default Portfolio;
